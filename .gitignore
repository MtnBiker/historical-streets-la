# See https://help.github.com/articles/ignoring-files for more about ignoring files.
#
# If you find yourself ignoring temporary files generated by your text editor
# or operating system, you probably want to add a global ignore instead:
#   git config --global core.excludesfile '~/.gitignore_global'

# Ignore bundler config.
/.bundle


/node_modules
/yarn-error.log

.byebug_history

# Compiled source #
###################
*.com
*.class
*.dll
*.exe
*.o
*.so

# Packages #
############
# it's better to unpack these files and commit the raw source
# git has its own built in compression methods
*.7z
*.dmg
*.gz
*.iso
*.jar
*.rar
*.tar
*.zip

# Logs and databases #
######################
# Ignore all logfiles and tempfiles.
/log/*
/tmp/*
!/log/.keep
!/tmp/.keep
*.log
*.sql
*.sqlite
*.dump
db/dumps/ # git showed that this folder was being considered. The files would not have been uploaded because of the above. Not working

# OS generated files #
######################
.DS_Store
.DS_Store?
._* # not _something.html.erb. Has to start with .
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Rails list below
*.rbc
capybara-*.html
.rspec
/log
/tmp
/db/*.sqlite3
/db/*.sqlite3-journal
/public/system
/coverage/
/spec/tmp
**.orig
rerun.txt
pickle-email-*.html
.ruby-version
.aprc # Awesome print preferences and I think this is dev only

# TODO Comment out these rules if you are OK with secrets being uploaded to the repo
config/initializers/secret_token.rb
config/secrets.yml

## Environment normalization:
/.bundle
/vendor/bundle

# these should all be checked in to normalize the environment:
# Gemfile.lock, .ruby-version, .ruby-gemset

# unless supporting rvm < 1.11.0 or doing something fancy, ignore this:
.rvmrc

# if using bower-rails ignore default bower_components path bower.json files
/vendor/assets/bower_components
*.bowerrc
bower.json

# Ignore pow environment settings
.powenv

# Ignore Byebug command history file.
.byebug_history

# Storing old copies of uploaded database, i.e., copies for uploading to Heroku
*.dump

# dotenv-rails gem for storing environmental variable. Clark p198
.env
.rubocop

# Ignore Spring files. p133 Hartl. Not sure I'm using Spring
/spring/*pid

# A map of how map is created and other macOS app files stored here for convenience
.intaglio
.numbers

# Specific files like notes
testNotes.txt
implementation_notes.md
coverage

# aliases to dot files
dot.*

* alias