<!DOCTYPE html>
<html>
  <head>
    <title><%= full_title(yield(:title)) %></title>
    <%= csrf_meta_tags %>
    <%= include_gon %>
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
  </head>

  <body>
    <!-- For Devise: Was only a yield before  But not getting formatting. I use similar in crores, but this is also duplicating to some extent what is on the index page for registration-->
    <nav class="navbar navbar-inverse"> <!-- Added navbar-fixed-top based on crores, but didn't give the styling. Something else is different TODO In crores this line is a header not nav, but changing it here didn't help. The navbar-header shows up if change from navbar-inverse to navbar-default -->
      <div class="container">
        <div class="navbar-header">
          <%= link_to 'Home Page', root_path, class: 'navbar-brand' %>
        </div>
        <div id="navbar">
          <ul class="nav navbar-nav">
            <li><%= link_to 'Home', root_path %></li>
          </ul>
          <ul class="nav navbar-nav pull-right">
            <% if user_signed_in? %>
              <li class="dropdown">
                <a class="dropdown-toggle" data-toggle="dropdown" href="#">
                  <%= current_user.name %>
                  <span class="caret"></span>
                </a>
                <ul class="dropdown-menu" role="menu">
                  <li><%= link_to 'Profile', edit_user_registration_path %></li>
                  <li><%= link_to 'Log out', destroy_user_session_path, method: :delete %></li>
                </ul>
              </li>
            <% else %>
              <li><%= link_to 'Log In', new_user_session_path %></li>
              <li><%= link_to 'Sign Up if you want to make additions or changes', new_user_registration_path %></li>
            <% end %>
          </ul>
        </div> <!-- end navbar -->
      </div> <!-- end container -->
    </nav>
    <div class="container">
      <% flash.each do |key, value| %>
        <div class="alert alert-<%= key %>">
          <%= value %>
        </div>
      <% end %>
    </div> <!-- end second container -->

    <%= yield :top_content %>

    <div class="container">
        <h1><%= yield :header %></h1>
      <%= yield %>
    </div> <!-- end third container -->
    
  </body>
</html>
