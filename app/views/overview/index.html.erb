<p id="notice"><%= notice %></p>
<% provide(:title) %>
<h1>Historic Street Names</h1>

<div class="row">
<!-- This is  _map_and_control except popupText isn't put in here. -->
  <div class="col card">
    <p class="font-bold-red">If no map shows up when you choose a map, zoom out. Or the map may not cover that region, so try another map, particularly if not near downtown.</p>
    <br/>

    <div id="map"></div>
      <script>
        $(document).on("turbolinks:load", function() {
           overviewMap(); // overviewMap() is in assets/javascripts/streets.js.erb
        });
      </script>
</div>  <!-- col for map -->

<%= render 'streets/basemap_selector' %>

</div> <!-- end row which is basically the page -->
