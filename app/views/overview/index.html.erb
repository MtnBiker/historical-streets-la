<p id="notice"><%= notice %></p>
<% provide(:title) %>
<!-- <h1>Historic Street Names in Los Angeles</h1> There already is a heading and space is a premium on this page -->

<div class="row">
<!-- This is  _map_and_control except popupText isn't put in here. -->
  <div class="col card">
    <p class="font-bold-red center">If no map shows up when you choose an overlay map, the map may not cover that region; so try another map, particularly if not near downtown.</p>
    <p class="center" id="show-line"><span>Click here to show all street segments at once</span>, then click on black lines to get information on the historic changes. Or use the time slider at bottom left to see when the name changes occurred.</p>

    <!-- <br/> -->

    <div id="map"></div>
      <script>
        $(document).ready(function() {
           overviewMap(); // overviewMap() is in assets/javascripts/streets.js
        });
      </script>
</div>  <!-- col for map -->

<%= render 'streets/overlaymap_selector' %>

</div> <!-- end row which is basically the page -->
